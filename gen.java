import java.util.*;
public class gen {
    public static void main(String[] args){
        boolean simStart = true;
        Scanner scanner = new Scanner(System.in);
        float[] subCoords = new float[3];
        for (int i = 0; i < 3; i++){
            subCoords[i] = scanner.nextFloat();
        }
        float[] pingCoords = new float[3];
        for (int i = 0; i < 3; i++){
            pingCoords[i] = scanner.nextFloat();
        }
        float timeElapsed = scanner.nextFloat();
        while (simStart){


        }
    }
}
